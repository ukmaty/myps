import * as React from "react";
import { css } from "@emotion/react";

const Cover = css`
display: grid;
padding: 10rem;
background-color: #ddd;
color: #fff;
`;

export default () => (
    <div css={Cover}>
        COVER
    </div>
)

